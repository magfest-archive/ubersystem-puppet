# THIS FILE IS GENERATED BY PUPPET
# DO NOT EDIT DIRECTLY, INSTEAD EDIT THE .erb template

<% if @band_email %>
band_email='<%= @band_email %>'
<% end -%>
<% if @band_email_signature %>
band_email_signature='<%= @band_email_signature %>'
<% end -%>

require_dedicated_guest_table_presence=<%= @require_dedicated_guest_table_presence %>
<% if @rock_island_groups %>
rock_island_groups = <%= @rock_island_groups %>
<% end -%>

[dates]
# if any of these are unset or '', the corresponding section will not show up in the group type's checklist
auction_start = '<%= @auction_start %>'
band_panel_deadline = '<%= @band_panel_deadline %>'
band_bio_deadline = '<%= @band_bio_deadline %>'
band_info_deadline = '<%= @band_info_deadline %>'
band_taxes_deadline = '<%= @band_taxes_deadline %>'
band_merch_deadline = '<%= @band_merch_deadline %>'
band_charity_deadline = '<%= @band_charity_deadline %>'
band_badges_deadline = '<%= @band_badges_deadline %>'
band_stage_plot_deadline = '<%= @band_stage_plot_deadline %>'
guest_panel_deadline = '<%= @guest_panel_deadline %>'
guest_bio_deadline = '<%= @guest_bio_deadline %>'
guest_info_deadline = '<%= @guest_info_deadline %>'
guest_taxes_deadline = '<%= @guest_taxes_deadline %>'
guest_merch_deadline = '<%= @guest_merch_deadline %>'
guest_charity_deadline = '<%= @guest_charity_deadline %>'
guest_badges_deadline = '<%= @guest_badges_deadline %>'
guest_autograph_deadline = '<%= @guest_autograph_deadline %>'
guest_interview_deadline = '<%= @guest_interview_deadline %>'
guest_travel_plans_deadline = '<%= @guest_travel_plans_deadline %>'

[enums]
<% if @guest_merch_enums -%>
[[guest_merch]]
<% @guest_merch_enums.each do |enum| -%>
<%= enum[0] %> = "<%= enum[1] %>"
<% end -%>
<% end -%>
